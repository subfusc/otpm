04.02.17:
 - Decide on encryption algorithm (AES, *fish)
 - Decide storage format (CSV, yml, marshalling, sqlite, ... ?)
 - Build general ruby structure (Gemfile, specfile, tests?)
 - Create API
 - Nail dependencies if any. (probably, because rotp)
